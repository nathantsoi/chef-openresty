server {
  listen   80;
  server_name  <%= node['hostname'] %>;

  access_log  <%= node['openresty']['log_dir'] %>/localhost.access.log;

  location / {
    root   /var/www/openresty-default;
    index  index.html index.htm;
  }
}
